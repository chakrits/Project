{
  "scripts": {
    "start": "node server.js",
    "postinstall": "npm --prefix mock-server/frontend install && npm --prefix mock-server/frontend run build",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/tests/mock-server/**/*.test.js"]
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "express-rate-limit": "^8.5.1",
    "js-yaml": "^4.1.0",
    "morgan": "^1.10.1",
    "path-to-regexp": "^8.2.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "jest": "^30.4.2",
    "supertest": "^7.2.2"
  }
}
